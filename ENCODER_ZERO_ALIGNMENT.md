# 编码器零位对齐 (对零) 详解

## 📌 为什么要对零？

**问题：编码器的0度 和 电机d轴的0度 不一定重合！**

```
┌────────────────────────────────────────────────────────┐
│                                                        │
│   编码器测量的角度 → encoder_angle                      │
│                                                        │
│   电机d轴(磁场轴) → rotor_zero_angle                   │
│                                                        │
│   转子物理角度 = encoder_angle - rotor_zero_angle      │
│                                                        │
│   如果不对齐，你给的角度指令就不准确！                  │
│                                                        │
└────────────────────────────────────────────────────────┘
```

---

## 🔄 对齐过程图解

### 步骤1: 上电
电机可能停在**任意位置**

```
        N
    ┌───────┐
    │       │
  W │   ⚙   │ U    ← encoder_angle = 任意值
    │       │       rotor_zero_angle = 未知
    └───────┘
```

### 步骤2: d轴强拖
`set_pwm_duty(0.5, 0, 0)` - 给d轴一个固定磁场，电机转到这里

```
        N
    ┌───────┐
    │   N   │        ← d轴磁场对齐这里
  W │   ⚙   │ U      电机转到这里停下
    │       │         (保持400ms)
    └───────┘
```

### 步骤3: 记录零位
`rotor_zero_angle = 当前encoder_angle`

```
        N
    ┌───────┐
    │   N   │        ← 记住这个位置！
  W │   ⚙   │ U      rotor_zero_angle = X
    │       │
    └───────┘
```

### 步骤4: 松开电机
`set_pwm_duty(0, 0, 0)` - 不给电，电机自由

---

## 🧲 什么是"d轴强拖"？

FOC中有两个正交轴：

```
            q轴 (转矩轴) → 产生转矩
                ↓
                ↓  ← 电角度方向
                ↓
    → d轴 (磁场轴) → 产生磁场
```

`set_pwm_duty(0.5, 0, 0)` 的含义：
- **d轴电压 = 0.5**
- **q轴电压 = 0**

给d轴充磁，电机就会转到让**d轴和磁场对齐**的位置！

---

## 🎯 为什么用基础矢量1？

SVPWM有6个基础矢量，构成六边形：

```
            V2(110)
               /\
          V3 /  \ V1(100)
            /    \
            \    /
          V4 \  / V6(001)
              \/
            V5(010)
```

**基础矢量1 (100)** 对应转子0度位置，这就是"零位"。

---

## 📊 对齐后的代码

```c
// 电流模式测试：给40%的q轴电流
motor_control_context.torque_norm_d = 0;
motor_control_context.torque_norm_q = 0.4;
motor_control_context.type = control_type_torque;
HAL_Delay(1000);

// 位置模式测试：转到90度位置
motor_control_context.position = deg2rad(90);
motor_control_context.type = control_type_position;
```

---

## 🎓 一句话总结

> **这段代码做的是：**
> 1. 强制把电机转到已知位置（d轴对齐）
> 2. 记录这个位置作为"零位"
>
> **相当于给编码器"定原点"，就像数控机床需要回零一样！**

---

## ⚡ 完整流程图

```
上电 → d轴强拖(400ms) → 记录零位 → 松开电机
                                      ↓
                    ┌─────────────────────────┐
                    │ 之后每次控制都这样计算：  │
                    │                         │
                    │ 转子物理角度 =           │
                    │   encoder_angle -       │
                    │   rotor_zero_angle      │
                    └─────────────────────────┘
```

