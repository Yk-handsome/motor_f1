你观察得非常仔细！这正是很多开源博客和教程中初学者最容易踩的“坑”——**作者在 CubeMX 截图时的最终配置，和他随手贴出来的某段早期测试代码，在参数逻辑上脱节了。**

既然截图里配置了**中央对齐（Center-aligned）**和**重复计数器（RCR）**，这就说明作者确实是奔着正规 FOC 去的，只是在第 4 节行文时忽略了文字解释。

同时，关于“为什么目标频率偏偏是 20kHz”，这是一个极其关键的工程经验值。

我将这两部分核心知识全部补充进了下方的 Markdown 文档中，并对计算公式的矛盾点做出了终极修正，你可以直接保存这份加强版的笔记。

---

# STM32 高级定时器 PWM 深度解析 (FOC 核心篇)

## 1. 核心拷问：为什么 FOC 的 PWM 频率目标总是 20kHz？

在无刷电机驱动中，20kHz 几乎是行业标配的“黄金频率”，这是基于**声学、热力学与单片机算力**三者妥协后的完美平衡点：

* **🎧 消除“啸叫” (声学原理)：**
人耳的听觉范围是 20Hz 到 20kHz。电机的线圈在受到 PWM 脉冲电流冲击时会产生微小的机械振动，就像扬声器一样。如果 PWM 频率是 10kHz 或 15kHz，电机就会发出极其刺耳的高频“尖啸声”。**将频率设定在 20kHz（或刚好超出 20kHz），超出了人耳听觉上限，电机就能实现“静音运行”。**
* **🔥 开关损耗 vs. 电流纹波 (热力学与电学)：**
* **频率越低**（如 5kHz）：MOS 管开关次数少，发热小（开关损耗低）；但电流纹波大，电机发热严重且扭矩不平滑。
* **频率越高**（如 40kHz）：电流极其平滑；但 MOS 管在一秒内要开关 4 万次，开关损耗剧增，驱动板极易过热烧毁。
* **20kHz** 正好兼顾了平滑的相电流与可控的 MOS 管发热。


* **💻 算力天花板 (控制工程)：**
FOC 算法包含大量的浮点三角函数、PID 运算和坐标变换（Clarke/Park 变换）。如果 PWM 是 20kHz，意味着中断频率最高也是 20kHz，单片机必须在 50 微秒（$\mu s$）内算完一整套 FOC 算法。对于 72MHz 的 STM32F1 系列来说，这几乎是算力的极限。如果频率设为 40kHz，CPU 将彻底跑飞。

---

## 2. 隐藏的真相：CubeMX 截图与测试代码的“参数矛盾”

根据你在教程 CubeMX 截图中观察到的 **中央对齐模式 (Center-aligned)** 和 **重复计数器 (RCR)**，我们可以倒推出博主那行测试代码 `0.3*72*1000/20` 存在的**逻辑矛盾**。

### A. 真实的频率输出 (频率减半现象)

如果 CubeMX 开启了中央对齐，定时器会进行上下双向计数。
博客中的计算值：


$$ \text{ARR} = 72,000,000 / 20,000 = 3600 $$

* **如果是边沿对齐（单向）：** 耗时 3600 个时钟，输出确实是 20kHz。
* **如果是中央对齐（双向）：** 耗时 $3600 \times 2 = 7200$ 个时钟，**实际输出的 PWM 频率变成了 10kHz！** 此时电机会发出明显的尖锐啸叫声。

### B. 正确的 FOC 参数配置公式 (修正版)

为了在中央对齐模式下真正实现 20kHz 的静音驱动，ARR 必须除以 2。正确的计算宏应修正为：

```c
// 目标频率 20kHz, 目标占空比 30%, 适用中央对齐模式
#define TIM_CLOCK       72000000 // 72MHz
#define TARGET_FREQ     20000    // 20kHz
#define DUTY_CYCLE      0.3f     // 30%

// 修正后的 ARR 计算 (除以 2)
uint32_t arr_value = TIM_CLOCK / (TARGET_FREQ * 2); 
// 实际 ARR = 1800

// 修正后的 CCR 计算
uint32_t ccr_value = (uint32_t)(arr_value * DUTY_CYCLE);
// 实际 CCR = 540

__HAL_TIM_SET_COMPARE(&htim1, TIM_CHANNEL_1, ccr_value);

```

---

## 3. FOC 高级配置：为何缺一不可？

以下是 CubeMX 截图中配置了但文字未详述的核心功能，它们是 FOC 闭环的基石：

| 核心配置项 | 状态 | 在 FOC 中的决定性作用 |
| --- | --- | --- |
| **Center-aligned Mode**<br>

<br>(中央对齐模式) | **必须开启** | 产生对称的三角波 PWM。这是生成标准 **SVPWM (马鞍波)** 的绝对前提，不对称的波形会导致电机抖动、谐波激增。 |
| **Dead Time**<br>

<br>(死区时间) | **必须配置** | 在互补的上下桥臂切换间隙，强制插入 0.5~1.5$\mu s$ 的全闭合时间，防止上下 MOS 管直通导致电源短路（炸管）。 |
| **Repetition Counter (RCR)**<br>

<br>(重复计数器) | **设为 1** | 让定时器每完成“一上一下”两次溢出后，才触发一次更新事件（Update Event）。避免 ADC 被重复触发，确保控制频率与采样频率完美同步。 |
| **Trigger Output (TRGO)**<br>

<br>(触发输出) | **Update Event** | 在中央对齐的顶点或底点（此时 MOS 管没有开关动作，电磁噪声最小）自动输出 TRGO 信号，纯硬件触发 ADC 去采集最干净的相电流，无需 CPU 干预。 |

---

## 4. 总结与实践指南

当您处于教程的第 4 节时，这行包含 `0.3` 占空比的代码仅仅是用来“点亮”硬件、用示波器验证波形的临时测试代码。

**您的实操 Checklist：**

1. 观察示波器：如果波形周期是 100 微秒（10kHz），说明博主代码和截图没对齐，请手动将 ARR 减半以达到 20kHz（周期 50 微秒）。
2. 放大波形交叉点：确认死区时间是否生效，CH1 和 CH1N 绝对不能同时为高电平。
3. 一旦进入闭环阶段，`0.3` 这个固定值将被抛弃，替换为由 PID 控制器输出并经过 SVPWM 调制后动态生成的实时占空比。